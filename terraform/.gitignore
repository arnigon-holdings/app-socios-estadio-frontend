# Terraform local cache + state (don't commit)
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfstate.backup
crash.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Lambda build artifacts (data.archive_file regenerates these on terraform plan)
modules/lambda/src/*.zip